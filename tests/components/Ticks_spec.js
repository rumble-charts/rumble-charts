'use strict';

const visibleProps = require('../helpers/visibleProps');
const generateRandomSeries = require('../helpers/generateRandomSeries');

const Ticks = require('../../lib/Ticks');

describe('Ticks', () => {

    // visibleProps(Ticks, {
    //     props: {
    //         tickVisible: ['g', 'tick'],
    //         labelVisible: ['text', 'label'],
    //         lineVisible: ['path', 'line']
    //     },
    //     seriesObjects3x5: generateRandomSeries(1, 5, {type: 'object'})
    // });

});
